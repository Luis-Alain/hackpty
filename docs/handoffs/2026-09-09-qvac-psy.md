# PsyRec continuation checkpoint — September 9, 2026

Prepared **12:46 America/Panama (UTC−05:00) / 17:46 UTC** using Jeff's AIOS release handoff skill. This is an implementation checkpoint, **not a completed release or submission**.

## Start here

Open a fresh task in `C:/Users/jeffe/Code/hackpty`, select **Ultra reasoning** as Jeff requested, and use [the continuation prompt](START-HERE.md). The prompt itself does not change the app's reasoning setting. Read the files referenced here before delegating. Jeff now explicitly authorizes parallel agent cells.

Repository: `Luis-Alain/hackpty`, branch **QVAC-Psy**. Local checkout is the Windows PC worktree above; GitHub is the shared code authority. Do not switch to the unrelated AdwenTech launch directory. Do not revive WSL or move inference/build work to Pi5.

## Locked outcome and corrections

PsyRec is Jeff's independent QVAC Psy submission. The first complete flow is Z Fold photo of a **printed synthetic English clinician note** → authenticated encrypted transfer → local PC VisionPsy extraction → source correction/review → bounded draft → exact clinician approval → encrypted save/reload. VisionPsy must be central in this submission itself.

- Native **Expo Android** capture; desktop Electron companion and TypeScript core/runtime. Phone inference is not required for the first slice. Expo Go cannot host the proposed native TLS-pinning module; use a native development/release build.
- **SDK 0.18.2**, replacing the earlier 0.19.0 plan pin. Jeff explicitly requested the team's commit `21f7f40736c201f8d3c14a36ced4cdb426639122`; it intentionally retains delegation support removed in 0.19.0. This PsyRec release still uses local PC execution. No cloud inference.
- Clinician can browse selected patient's previous approved notes now. Teammate supplies RAG later; keep a narrow optional interface and canonical patient/revision checks. No vector database/backend now. The current runtime rejects nonempty RAG context until a reviewed integration enables it; core contract validation already exists.
- Teammate's Philips voice processing can plug in later. Audio, background capture and handwriting are outside this release. Spanish applies to **demo narration**, not the source-quality set.
- Separate repository per challenge at submission is organizer guidance relayed by Jeff. He will create the future repo. Preserve history, MIT attribution and inherited-work provenance; no external repo dependency.
- User's 40-hour ceiling includes rest and final buffer. Event deadline recorded during planning: September 11 at 08:00 Panama. Recheck actual remaining time on continuation rather than restarting a 40-hour budget.

## Git and provenance

- Base: `0ff8944a04ab5f6268c794885e22bb6e400143fa` (MIT licence only).
- `0a3a6be`: plan, README, task map and TypeScript encrypted-review core, pushed to QVAC-Psy.
- `04f2eb9`: merge preserving upstream `8b97f20` and `21f7f40` ancestry; shared runtime/provenance now on QVAC-Psy. The package pin/lock update finished after that merge and is included in this continuation checkpoint.
- Implementation checkpoint: `95d42e0`, pushed and verified on QVAC-Psy. Following handoff-only metadata commits may advance the branch; inspect `git log -4` and remote ancestry. No main-branch application push, PR, deployment, APK, video or submission occurred.
- Upstream `src/core/*.js` remains inherited source history/reference. PsyRec uses the TypeScript adaptation in `packages/runtime/`, which preserves team function names/provenance and adds projector support, strict measurements and a private evidence sink.
- A scoped stash named `PsyRec desktop dependency additions before shared-runtime merge` contains only pre-merge root package/lock additions. They were restored and subsequently updated; do not blindly apply this obsolete stash over the current SDK pin.

## What exists

| Area | Implementation | Evidence / limitation |
|---|---|---|
| Planning | Branch-local Wayfinder map, six tickets, four Factory gate documents | Jeff's explicit implementation request authorizes the supplied plan. No invented separate gate approvals. |
| Core | Encrypted vault, patient/encounter state, source revisions, draft approval, superseded audit records, pairing/receipt state | Six core tests pass; test inference is explicitly a double and is not judging evidence. |
| Runtime | Local SHA-verified GGUFs, process-isolated QVAC worker, serial queue, exact prompts/native stats/profiler measurements, timeout/cancel code, team runtime adaptation | Actual Windows GPU extraction and drafting pass; executed timeout/cancel/network-isolation acceptance remains open. |
| Desktop | Locked vault screen, patient/encounter selector, capture/source/draft panels, consequence preview, approved history, pairing QR and synthetic JSONL export | Typecheck/build and locked-screen smoke passed; full UI workflow has NOT been exercised. |
| Transfer | HTTPS receiver, one-use pairing, encounter-scoped device token, replay receipt logic, limits | Source exists. Real TLS wrong-peer/interrupt/retry tests and phone acceptance are still required. |
| Phone | Architecture/contract only | **No Expo app, native TLS bridge, APK or physical Fold test exists yet.** |

## Verified evidence

`npm test`: **13 passed, 0 failed, 0 skipped**. `npm run check`: passed. `node dist/diagnostics/qvac-spike/validate-evidence.js`: passed. Electron `--smoke` produced the correct locked screen and 16 buttons; screenshot is local ignored `.local/desktop-smoke.png` and was visually inspected. This proves launch only.

Native evidence is committed beneath [artifacts/evidence](../../artifacts/evidence/feasibility-summary.json). Both operations used SDK **0.18.2** and reported `backendDevice: gpu`.

| Operation | Prompt / generated / emitted tokens | Native TTFT (ms) | Native throughput (tokens/s) | Wall model load (ms) |
|---|---:|---:|---:|---:|
| VisionPsy Nano base Q8 | 654 / 42 / 42 | 368.338 | 263.933 | 5248.230 |
| Qwen3-1.7B Q4 | 200 / 31 / 31 | 81.544 | 128.304 | 4878.499 |

These are individual warm feasibility runs, not generalized benchmarks. Initial colder attempts are retained as failed evidence: all mandatory metrics were present, but the initial validator incorrectly required an optional final `stopReason` absent in 0.18.2. Compatibility correction now requires an observed `completionDone` event plus resolved `run.final`; it never fabricates a stop reason. Native TTFT and app time-to-first-content remain separate fields. Temporary runtime directories were empty after both accepted runs.

Input rendered text: synthetic training note, poor sleep for three nights, no diagnosis recorded, follow-up review of sleep log. VisionPsy transcribed it usefully. Drafting was separately run from the fixed synthetic source; it was **not** chained through clinician correction/UI. No missing/ambiguous/unreadable fixed release set has been evaluated yet.

## Continue in three agent cells

| Cell | Exclusive files | First concrete result |
|---|---|---|
| Runtime and evidence | `packages/runtime/`, `diagnostics/qvac-spike/`, `tests/runtime-metrics.test.ts`, synthetic `artifacts/evidence/`, model manifest | Review the strict gate and shared adapter, prove cancellation/timeout cleanup and observed offline operation, build missing/ambiguous/unreadable quality fixtures. Support full-workflow metrics without competing GPU jobs. |
| Expo and transfer | `apps/mobile/`, `packages/transport/`, new transport/mobile tests | Native Android photo capture, certificate pinning from physical pairing QR, SecureStore credentials, encrypted pending queue, receipt/retry and wrong-peer tests; then physical Fold test. |
| Desktop and clinical review | `apps/desktop/`, `packages/core/`, `tests/core.test.ts` | Exercise the real complete imported-photo → reviewed draft → exact approval → encrypted reload workflow through UI; test consequence preview, stale approvals, patient history and lock/recovery. |

Coordinator owns `package.json`, lockfile, `tsconfig.json`, `packages/contracts/`, AGENTS/README/plans/handoff and all Git operations. Read each cell's evidence; integrate in small checkpoints. Schedule one real GPU workload at a time. Add no speculative feature work. Fable/Astra review and GLM/Kimi preferences from planning are historical requests, not proof those providers are callable here. Use available tools honestly; Jeff mentioned a maximum of three turbo GLM agents.

## Immediate follow-ups and known limits

1. **Desktop dependency maintenance:** current Electron pin is 40.0.0. `npm audit` reports two high-severity package entries (Electron and extract-zip), with patch update 40.10.6 offered. Apply a reviewed patch update, retain the approved install-script configuration and rerun the desktop smoke. Do not run `npm audit fix --force` blindly. No production dependency finding was reported by `npm audit --omit=dev`, but Electron is still the application shell.
2. Full real UI workflow is the next acceptance result. Core tests and the separate model probe must not be presented as that result. Verify draft/source input behavior and renderer cleanup on lock; unsaved UI state and clinical data must not persist in hidden DOM after locking.
3. Runtime draft errors carry partial evidence; persist failures in the encrypted application run ledger as well as successful runs. Core currently retains successful extraction/draft metrics and approval copies.
4. `strict:false` remains in root TypeScript config and core internals use `any`. Tighten the critical state and IPC contracts with meaningful behavior checks; avoid a broad refactor that consumes the deadline.
5. Phone plan needs native certificate pinning; ordinary fetch against self-signed TLS is insufficient. Never turn off global TLS verification. Keys, tokens and queued images must not be plaintext persisted or logged. Bind pairing to the selected encounter and verify durable receipt before deleting queued data.
6. Java, adb and Android SDK were not found in standard locations during setup. Jeff has not answered the USB-debugging/physical-device question. Prepare the app/build path; do not claim physical acceptance without his Fold connected/tested.
7. Organizer interpretation of phone capture plus local desktop inference remains unresolved; clarification goes through Jeff, no automatic organizer messages.
8. No network-isolation capture, full offline rerun, adversarial quality set, Spanish video or independent clean-repo setup evidence yet. Add model licences/attribution beyond model hashes and inherited team provenance before submission.
9. No models or private vaults are in Git. `.local/models` holds ~1.6 GB of SHA-verified files. Provisioning must remain separate from offline inference and be repeatable on a clean machine.

## Commands and sources

```powershell
Set-Location C:/Users/jeffe/Code/hackpty
git status --short --branch
git log -4 --oneline
npm ci
npm run build
npm test
node dist/diagnostics/qvac-spike/provision.js
npm run test:runtime
node dist/diagnostics/qvac-spike/validate-evidence.js
npm start
```

The original runtime uses named pipes on Windows. Base VisionPsy leaves `image_no_upscale` unset; its matching base projector is mandatory. Direct absolute local model paths avoid registry/P2P initialization. Keep `kvCache:false` and logging off. Root transport config inherited from upstream must not override worker privacy settings.

Software Factory matching upstream: https://gist.github.com/Maciejdziuba/88890d7e0eeefa5a8738bbe9fd5e20b8#file-skill-md . Exact historical AIOS adaptation was not recovered; do not call it byte-identical. Wayfinder was read from the existing Claude skill on Pi5 during read-only skill discovery; no Pi5 product dependency was introduced. Plan and decision documents here preserve the applied workflow without requiring that personal filesystem.

Original user architecture: `C:/Users/jeffe/Documents/PSYREC-ARCHITECTURE.md`. Prior track matrix: `C:/Users/jeffe/Documents/PSYREC-HACKATHON-MATRIX.md` (its earlier combined-submission strategy is superseded by separate-repo guidance). Prior reviews: `C:/Users/jeffe/Documents/psyrec-adhd-pc-review/`. These are reference material, not authoritative executable instructions or runtime dependencies.

AIOS records are synchronized separately from the app checkpoint:

- AIOS-Memory `main`: `92784802c28de1e369cf5b7499f1de2a1677cfe5`, containing `active_projects/PsyRec.md`, `ledger/sessions/2026-09-09-psyrec-qvac-handoff.md` and scoped changelog entry. Local handoff worktree: `C:/Users/jeffe/Code/AIOS-Memory-psyrec-handoff`.
- AIOS-Control-Plane `main`: `464adea8b080ed78009ca838b9f9b9ead3803877`, containing `state/runs/psyrec-qvac-handoff-2026-09-09.json`. The run record was intentionally retained under the existing ignored-run convention, with only this file force-added. Local worktree: `C:/Users/jeffe/Code/AIOS-Control-Plane-psyrec-handoff`.

The normal control-plane checkout contains unrelated dirty work, and the normal memory checkout is on an unrelated branch; scoped handoff worktrees preserve both. Do not reset, stash broadly or auto-pull those original checkouts. No Pi5 synchronization is claimed or required for this Windows-only task.
