# Paste into the fresh Ultra session

Continue implementing PsyRec in `C:/Users/jeffe/Code/hackpty` on `QVAC-Psy`. Read `AGENTS.md`, `docs/handoffs/2026-09-09-qvac-psy.md`, the Factory status and Wayfinder map first. The handoff is an unfinished implementation checkpoint, not a completed release.

I authorize a coordinator and up to three parallel agent cells with the disjoint ownership defined in the handoff: runtime/performance evidence, Expo/phone transfer, and desktop/clinical review. The coordinator owns integration, root dependencies/contracts and Git. Use the selected Ultra reasoning for coordination; preserve model names and be honest about unavailable GLM/Kimi/Fable providers. Run only one real GPU job at a time.

Complete the smallest independent QVAC Psy release: Fold photo of printed synthetic English clinician note → encrypted paired transfer → VisionPsy on my Windows PC → source correction/review → bounded draft → exact approval → encrypted save/reload. Include selected-patient approved history. RAG and Philips voice are future teammate integrations, not new release implementations. SDK 0.18.2 and team commit 21f7f40736c201f8d3c14a36ced4cdb426639122 are intentional. Use TypeScript and native Expo Android; no Pi5 or cloud inference.

Maintain the dedicated performance evidence owner throughout. Exact prompts, model/load configuration, token counts, TTFT and throughput are mandatory; missing fields fail acceptance. Full user workflow evidence is mandatory in addition to existing real SDK probes. Keep private run records encrypted and publish only synthetic evidence.

Inspect current Git/remote status, fix the documented Electron patch issue, then execute the cells. Keep README/task status current and commit/push reviewed checkpoints to my branch so teammates can see progress. Preserve upstream history and portability to the separate submission repository I will create later. Do not merge to main, submit, contact organizers or create that repository automatically. Stay within the remaining hackathon time and leave a final buffer.
