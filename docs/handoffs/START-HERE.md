# Paste into the next Ultra session

Continue PsyRec in `C:/Users/jeffe/Code/hackpty` on `QVAC-Psy`. Read `AGENTS.md`, `docs/handoffs/2026-09-09-aios-continuation.md`, `docs/plans/qvac-psy/00-status.md` and `.scratch/qvac-psy/map.md` first. This is an unfinished implementation checkpoint. Inspect current Git/remote and device state; do not repeat completed Electron patching, Expo linking or iOS simulator validation.

I authorize a coordinator plus up to three disjoint cells: dedicated runtime/performance evidence, Expo/phone transfer, desktop/clinical review. The coordinator owns integration, root dependencies/contracts, documentation and Git. Use the selected Ultra reasoning for coordination; preserve actual provider/model names and be honest about unavailable GLM/Kimi/Fable. Run only one real GPU job at a time.

Complete the smallest independent QVAC Psy release: physical Fold photo of a printed synthetic English clinician note → encrypted paired private-Wi-Fi transfer → VisionPsy on my Windows PC → source correction/review → bounded draft → exact human approval → encrypted save/reload and selected-patient approved history. Observe phone-side encrypted queue restart, interrupted retry, certificate rejection and deletion only after the matching durable receipt. The latest built Android APK has not been verified installed. Existing SYN-HW-002 physical evidence remains provisional because of observer failure, missing assisted lock-purge/phone observations and unconfirmed paper-versus-display capture; do not relabel it as accepted or repair history.

Maintain the performance evidence owner throughout. Exact prompts, model/load configuration, native token counts, TTFT and throughput with units/methods are mandatory; missing fields fail acceptance. Full human workflow evidence is required in addition to the existing automated desktop and real SDK probes. Keep private run records encrypted and publish only reviewed synthetic evidence. Preserve the first unconfirmed-provenance private vault; do not export or reconstruct its missing raw output. Do not retry the previously rejected temporary-file cleanup through another tool.

SDK 0.18.2 and team commit 21f7f40736c201f8d3c14a36ced4cdb426639122 are intentional. Use TypeScript with the existing native Android bridge and local Windows inference. The committed iPhone port has a finished EAS simulator build; teammate signing and physical testing follow apps/mobile/docs/ios-xcode-handoff.md. RAG, Philips voice and the unimplemented phone-local inference plan are future work, not new release implementations. Generated handwriting-style fixtures are exploratory evaluation inputs, not trained models or general handwriting acceptance. No Pi5 or cloud inference.

Keep README/Factory/Wayfinder current and commit/push reviewed checkpoints to my branch. Preserve upstream history and portability to the separate submission repository I will create. Do not merge to main, submit, contact organizers or create that repository automatically. Do not restart the original 40-hour ceiling. The deadline is September 11 at 08:00 Panama; preserve 04:00–08:00 that morning for verification, handoff and my submission.
