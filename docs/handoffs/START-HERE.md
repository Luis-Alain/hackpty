# Paste into the next Ultra session

Latest: read [the MedPsy chart-review checkpoint](2026-09-10-medpsy-chart-review-checkpoint.md) first, then [the binding coordinator decisions](2026-09-10-coordinator-decisions.md) (D1–D10). This coordination shipped the chart-review contract, a 16-case held-out suite frozen after I confirmed 75 of 75 items, the application-bound chart-review core and desktop panel, the runtime review operation, and a held-out evaluation across four configurations. Read [CHART-REVIEW-EVALUATION.md](../../diagnostics/qvac-spike/CHART-REVIEW-EVALUATION.md) next — its results table and verdict are the record. No configuration meets the predeclared targets (strict recall ≥ 0.80, zero forbidden-term hits, 100% first-pass validity): MedPsy-1.7B structured scored strict recall 0.133, thinking scored 0.133; Qwen3-1.7B Q4_0 structured (current generic) scored 0.333, thinking scored 0.100. The generic model measured higher than the medically specialized one on this suite. Thinking mode cannot be combined with the strict json_schema grammar on SDK 0.18.2, and the `/no_think` suffix does not suppress thinking; see [MEDPSY-RUNTIME.md](../../diagnostics/qvac-spike/MEDPSY-RUNTIME.md) for the probe evidence.

Continue PsyRec in C:/Users/jeffe/Code/hackpty on QVAC-Psy. Read in this order before doing anything else: AGENTS.md, docs/handoffs/2026-09-10-medpsy-chart-review-checkpoint.md, docs/handoffs/2026-09-10-coordinator-decisions.md, diagnostics/qvac-spike/CHART-REVIEW-EVALUATION.md, docs/plans/qvac-psy/00-status.md and .scratch/qvac-psy/map.md. This remains an unfinished release; SDK 0.18.2 and team ancestry 21f7f40736c201f8d3c14a36ced4cdb426639122 are unchanged. Inspect current Git/remote/device state before doing more work. A separate Codex app-server session is a live peer coordinator on the same working tree (device identity, Android startup fix, mobile history/Hyperswarm pairing) — stay out of that lane, and if you need to describe its commits, describe them only from its own docs; never invent details about them.

Committed this coordination: `packages/contracts/chart-review.d.ts`; the frozen, human-confirmed 16-case held-out suite plus its 4-case development split (`suite/REVIEW-LOG.md` section 4 has the frozen hashes); `packages/core/chart-review.ts`, the desktop panel/IPC and `docs/CHART-REVIEW.md`; printable pages for every suite case; the runtime review operation in `packages/runtime`, the evaluation harness, scorer, verifier and all evidence records under `artifacts/evidence/`; the `review` role entry in `MODEL-MANIFEST.json` for MedPsy-1.7B (reasoning disabled, strict schema); the HyperFrames demo video project first cut (2:57.7) with its results scene bound to the verified evaluation; and the OCR rerun `20260910T0312Z` on the two development-split bitmap fixtures (aggregate WER 0.056, CER 0.035, 4 of 15 clinical rules failed).

Do not redo what is already measured and frozen. Do not re-run the four-configuration evaluation — the numbers are final for suite v1 and independently verified (`verify-chart-review-evaluations.js` prints `passed-evidence-bindings` on all four held-out runs). Do not re-freeze or edit the held-out suite — I already confirmed 75/75 items; CR-15 P2 and CR-06 P1 wording remain open per D4, do not silently resolve them, and CR-16 "omit" wording is a documented residual risk, not a bug to fix quietly. Do not download or re-provision MedPsy — the provisioning receipt and sha256 are already verified. Do not repeat completed Electron patching, Expo linking or iOS simulator validation from earlier checkpoints.

Open decision for me: keep MedPsy-1.7B in the `review` role as the approved Track 2 direction with the Qwen comparison disclosed, or switch the role to Qwen3-1.7B explicitly because it measured higher on this suite. Neither a silent fallback nor a unilateral pick by the next session — surface both options with their evidence and wait for my call.

Physical work still open: my assisted physical session with the connected Samsung SM-F966B follows docs/PHYSICAL-ACCEPTANCE-CHECKLIST.md — the chart record pair as history and current visit, phone lifecycle observations, source correction, chart review, exact approval, lock/reload, then camera runs of suite cases CR-09, CR-13, CR-15, CR-05, CR-06. Held-out cases used on camera become seen; those runs are demonstration and human-benefit evidence, never a re-measurement of the frozen held-out numbers. The OCR rerun above only covers the two development-split bitmap fixtures; three paper-photo fixtures remain missing until this physical session provides them — do not describe the bitmap numbers as paper-photo or handwriting results. The demo video's screen captures are placeholders until the physical session supplies them; only then do the final render, self-review and human listening pass follow.

Deadline: September 11 at 08:00 Panama; preserve 04:00–08:00 that morning for final verification, handoff and my submission. Do not restart the original 40-hour ceiling.

Standing project rules unchanged: PC inference only (no Pi5, no cloud inference), one real GPU job at a time, synthetic data only with SYNTHETIC / NOT A REAL PATIENT labeling, no diagnosis/therapy/treatment-advice framing, no merge to main, no submission, no organizer contact and no new repository created automatically. This branch must stay portable to the future submission repository — no personal-path requirements, no model binaries in Git.

Runner attribution stays exact and is never relabeled: Kimi Code CLI 0.40.1 alias kimi-code/k3 (self-report "Kimi / Moonshot AI") wrote the implementation cells; Z.ai requested glm-5-turbo, runtime-reported glm-5.3-flash, ran adversarial review; Claude Sonnet wrote fix round 3 only, under the same brief files, after Kimi's usage quota was exhausted (D9) — its commits name Claude Sonnet, never Kimi; Claude Fable 5.1 is coordinator/judge; Codex CLI 0.153.0 (gpt-6-astra) built the demo video project. Use only actually available provider/model names; never substitute a label for an unavailable one. Never claim acceptance, clinical validity or a MedPsy benefit beyond what CHART-REVIEW-EVALUATION.md measured: the honest verdict is that no configuration meets the targets and the generic Qwen3-1.7B scored higher.
