# PsyRec continuation after physical desktop recovery

September 9, 2026, approximately 13:55 Panama / 18:55 UTC. This remains an **unfinished release checkpoint**. Read AGENTS.md, Factory status and Wayfinder before continuing; the original handoff is historical.

## Reviewed and pushed

- `c63aa67`: Electron 40.10.6 patch with scoped install-script permission.
- `ce16d1e`: runtime lifecycle/quality measurements and provenance.
- `13ebf49`: native Android camera, encrypted pending queue and pinned private-LAN transfer.
- `7f58a88`: withdraw prematurely labelled physical metadata; no patient photo/text was published.
- `028ab6b`: connected desktop workflow, raw encrypted run output retention, strict evidence, two generated handwriting-style fixtures and real local evaluations.
- `e8e8515`: verified Expo link to `@hackpty/psyrec-capture`, project `b57b65cb-66a6-454f-8301-72259ee2a828`; local Android identity/builds preserved. Receipt-only completion marker prevents recapture into completed encounters.
- `62645b0`: fresh source archive/dependency checks on this Windows machine, not an independent-machine test.
- `5503d23`: fix logo `#` navigation causing "Untrusted window"; main-frame trust still rejects other documents and queries.
- `dd79b52`: explicitly attribute assistant navigation checks; retain mandatory human source review/approval; separate exploratory physical evidence from primary printed-note acceptance.

Root suite: **32 passed, no failures/skips**. Desktop and native metrics gates pass. Default physical release gate still fails because accepted primary evidence is absent. No main merge, submission, organizer contact or new repository occurred. SDK 0.18.2 and required team ancestry remain intentional.

## Live recovery and privacy

Jeff requested taking control because desktop navigation was stuck. The Windows computer-use helper failed to initialize twice with a sandbox ACL error. App-owned diagnostic inspection repaired the harmless URL fragment and removed the live brand link. Assistant navigation then verified selected-patient history, other-patient exclusion, renderer purge and exact encrypted reload. It did not change source text, generate a new draft or approve a record. The synthetic approval remains Jeff's observed input.

The first physical vault, `.local/desktop-workflow/physical-fold-1`, contains an earlier handwritten source whose provenance is unconfirmed. Preserve it encrypted; do not export it or use its history in synthetic evidence. Its original raw extraction was not retained before source correction, so do not reconstruct that output.

The fresh vault `.local/desktop-workflow/physical-printed-1` contains a known generated SYN-HW-002 photo and its approved record. The image is confirmed synthetic, but whether Jeff photographed paper or a display remains unanswered. Label it as generated handwriting-style exploratory capture. Do not set primary printed-note acceptance true without evidence. User interaction should remain on this fresh vault.

The diagnostic observer initially reported success but reported failure after assisted navigation. Preserve that observed failure; do not infer false from a restart or change the old status to pass a gate. Exact inference records remain encrypted and retained. User-visible recovery succeeded, while strict workflow acceptance is still under review.

## Continue without losing evidence

1. Dedicated runtime/performance owner completed independent review of the two physical run records, attachment/output hashes, exact prompts, load configuration, native tokens/TTFT/throughput and observed process/socket window. [Published review](../../artifacts/evidence/physical-performance-binding-review.json) and [provisional workflow](../../artifacts/evidence/physical-fold-exploratory-provisional.json) preserve the unresolved failures. Verification uses only published artifacts and never writes by default. Only known synthetic material was published.
2. Desktop owner added observer generation/drain protection and a real encrypted-vault regression for delayed views crossing lock. This does not establish the retrospective cause of the original failure or repair it. The truthful provisional workflow is `.local/desktop-workflow/physical-provisional.json`; actual final failure and missing encrypted purge event remain. Human clinical input and assistant navigation must remain distinguishable. Any future clean run must use the fixed application and complete evidence.
3. Primary printed-note physical acceptance, Android queue restart/interrupted retry, latest APK installation and enforced offline evidence remain open. Core phone transfer uses Wi-Fi and does not require a cable; device debugging is a separate test dependency.
4. Latest local APK at the Expo-link checkpoint has SHA-256 `084e23b7a10899a472691c087460e4be91bf206280690b2386e1ade089716818`; the Fold was still running the earlier installed APK. Keep installed/build identities separate.
5. Phone-local inference is not implemented. The bounded Android plan records actual SDK/Bare dependencies and requires its own measured mobile runtime profile; never label phone CPU/worklet evidence as Windows GPU evidence.
6. Two old unmarked synthetic runtime temporary directories remain because automatic approval review rejected their scoped deletion as "blocked by policy". Do not retry deletion through another tool or shell. Stock SDK 0.18.2 exposes file-path image attachments; abrupt OS termination can leave a plaintext temporary image until eligible recovery.
7. New iPhone source/config files appeared from an unidentified concurrent worker outside these three cells. Preserve them untouched and unstaged unless their owner coordinates review. Do not fold them into an Android checkpoint by broad `git add`.

Keep a coordinator plus the three authorized disjoint cells and only one real GPU job at a time. Do not restart the original 40-hour budget. Event deadline remains September 11 at 08:00 Panama; preserve the final 04:00–08:00 verification/user-submission buffer. Spanish narration exists; final video and independent-machine setup are not claimed.
