# Coordinator decisions (binding for all cells) — 2026-09-09 23:00 UTC

D1. Evidence budget for the chart-review packet: total evidence text (current + historical excerpts) at most 6000 characters; per historical record one sentence-bounded head excerpt of at most 2000 characters; at most 6 historical records; omissions listed in coverage.omitted. Reason: ctx_size 4096 tokens must leave room for reasoning plus the final JSON. Cell C enforces it in buildChartReviewPacket; Cell A enforces the same total in QvacRuntime.reviewChart validation (replace the 12000 figure in Cell A's brief with 6000). Suite case CR-16 assumes exactly this budget.

D2. Scorer standard: strict coverage stays as written in SUITE-SCHEMA.md (a gold item is covered only by an output finding whose statement contains all requiredTerms, whose kind matches, and whose evidenceIds are a subset of permittedEvidenceIds). One output finding may satisfy several gold items only if it meets each item's rule independently. The term-only measure is always reported next to strict coverage so combined-sentence misses are visible. Do not widen gold permitted sets.

D3. The negation guard in SUITE-SCHEMA.md section 5.4 (cue before the match, same sentence) is mandatory in the scorer. A violation rate from a scorer without it must not be quoted.

D4. Human sign-off: draft-v1.json stays frozen:false until Jeff signs REVIEW-CHECKLIST.md (regenerated after the review). Runs before that are labelled goldConfirmedByHuman:false and are provisional. CR-15 P2 and CR-06 P1 remain in the suite pending Jeff's decision; CR-16 "omit" wording is a documented residual risk.

D5. Model roles: production MODEL-MANIFEST.json gains a `review` role entry for MedPsy-1.7B q4_k_m imatrix only after Cell A's A2 verification shows it loads and returns valid structured JSON; the coordinator makes that edit. Until then the desktop chart-review path is exercised with test doubles.

D6. Runner attribution: Kimi Code CLI 0.40.1 alias kimi-code/k3 (self-report "Kimi / Moonshot AI") for implementation; Z.ai requested glm-5-turbo, runtime-reported glm-5.3-flash, for review; Claude Sonnet as thin driver/harness; Claude Fable 5.1 as coordinator/judge. Evidence never relabels any of these.

D7 (2026-09-10 00:20 UTC). GPU lease and foreign GUI processes. nvidia-smi lists Cursor.exe (PID 65412, a code editor, no inference) as a GPU process on this laptop, and diagnostics/qvac-spike/gpu-lease.ts aborts on any foreign GPU compute PID, so no MedPsy job can start. Cell A amends gpu-lease.ts as follows, keeping the guard honest: (1) abort on any foreign process whose executable is node.exe, electron.exe, bare.exe, python*.exe, or whose command line matches the runtime worker, as today; (2) for any other foreign GPU compute PID, do NOT abort but record it in the lease evidence as observedForeignGpuProcesses [{pid, name, usedMemory}] together with the raw nvidia-smi lines, and state in the lease method string that a non-runtime GUI process shared the GPU during the run; (3) keep the 1 s monitor and the before/after checks. Every run record produced while a foreign GUI process was present must carry that observation; the evaluation report must disclose it. Closing Cursor restores the strict behaviour and is preferred when Jeff is present; the coordinator will not close it on his behalf.

D8 (2026-09-10 00:55 UTC). Evaluation configurations after A2. On SDK 0.18.2 the strict json_schema grammar cannot be combined with thinking for this model (verify4 probe d: "Unexpected empty grammar stack"), and the /no_think suffix does not suppress thinking (probe c). Therefore A6 compares four configurations with the identical prompt module, packet binding and scorer:
 (i)  MedPsy-1.7B q4_k_m imatrix, reasoning_budget 0 + strict json_schema (the production candidate; ctx_size 4096, predict 1536).
 (ii) MedPsy-1.7B in thinking mode WITHOUT grammar: reasoning_budget 1024, predict 2560, ctx_size 8192, captureThinking true; the final JSON is parsed from contentText and validated with the same strict validator; first-pass validity is a scored outcome, not a filter. Disclose the different ctx_size and budgets.
 (iii) Qwen3-1.7B Q4_0 (production asset), reasoning_budget 0 + strict json_schema, same prompt and budgets as (i): the current generic configuration.
 (iv) Qwen3-1.7B Q4_0 in thinking mode without grammar, same settings as (ii): the matched control for medical specialization (quantization difference Q4_0 vs q4_k_m imatrix disclosed).
Order: dev-v1 on (i) first, iterate prompt wording on dev only, freeze the prompt module hash in the protocol, then run held-out draft-v1 on (i)-(iv) one GPU job at a time. History-question cases (CR-11, CR-12 and any case with a question) run through the query operation with the same model under test. Predeclared targets stay: gold recall >= 0.80, zero forbiddenTerms hits, first-pass schema validity 100 percent, abstention correct on every abstention case. Every record labels goldConfirmedByHuman false until .local/cells/shared/SUITE-FROZEN.md exists. MedPsy-4B only after (i)-(iv) are complete and verified.

D5 executed (2026-09-10 00:55 UTC): the production MODEL-MANIFEST.json now carries a `review` role entry for MedPsy-1.7B q4_k_m imatrix (reasoning disabled, strict schema), because A2 showed it loads and returns valid schema-bound JSON in that configuration. Adoption for release still depends on the A6 gates.

D9 (2026-09-10 02:52 UTC). Kimi Code CLI returned "403 You've reached your 5-hour usage limit" when resuming Cell A for fix round 3. Until the quota resets, small bounded fix rounds are written by a Claude Sonnet agent under the same brief files, verified by GLM as before, and every commit message names the actual writer. No relabeling: work written by Sonnet is attributed to Claude Sonnet, not to Kimi.

D10 (2026-09-10 03:05 UTC). Fix round 3 judged PASS by the coordinator. GLM's FAIL on items 3 and 4 was an artifact of the review material: diagnostics/qvac-spike/verify-chart-review-evaluations.ts and chart-review-evaluation.ts are untracked files, so `git diff` showed no change for them, and the evaluation note was truncated to 16 lines. Direct inspection confirmed: the verifier asserts key presence for post-fix records (stopReasonKeyGuaranteed or prompt version newer than v4) and prints a named WARNING for the retained (i)/(iii) records; the harness sets stopReasonKeyGuaranteed: true; the note cites the generated-token averages (191/1536 and 273/1536); the worker writes stopReason: final.stopReason ?? null; 51 owned tests pass; the read-only verifier passes all four final runs (16 warnings each on the two retained structured runs). GLM's valid point that the new test reconstructs the worker's object shape rather than invoking the worker is accepted as a documented limitation.
