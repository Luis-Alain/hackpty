# Accuracy and approved-note query checkpoint — September 9, 2026

This is an unfinished implementation checkpoint on QVAC-Psy. Continue in the hackpty repository; preserve required team ancestry 21f7f40736c201f8d3c14a36ced4cdb426639122 and SDK 0.18.2. The coordinator owns integration, root contracts/dependencies, documentation and Git. Keep the dedicated performance evidence owner and at most three disjoint cells. No GLM/Kimi/Fable was used or substituted. One real GPU job at a time.

## Delivered behavior

- The desktop has an initially blank human-reference panel under Check the source. A direct human save binds exact synthetic reference text to the capture SHA and original extraction output/run, then records WER/CER, edit counts and normalization methods in an append-only encrypted ledger. Corrected source and missing raw output are never substituted as the extraction. This is not a blinded reference; clinical meaning is not automatically scored. See [user instructions](../ACCURACY.md).
- Experimental questions search the selected patient's current approved revisions. Application code selects at most six whole-sentence passages (800 characters each), QVAC returns authorized source IDs through native JSON Schema, and the app obtains exact displayed text from canonical records. Full context, raw output and performance are encrypted separately from approved history. Reauthorization rejects stale sources, lock/patient changes discard visible results, and coverage identifies omissions. There is no generated SQL, embedding index or automatic clinical approval. A visible notice explains that results may omit relevant notes.
- The isolated preload now observes actual trusted events itself; the renderer cannot set a trusted observation flag. Gold references require a one-use trusted click grant bound to exact text and encounter. These changes strengthen future evidence and do not repair historical failures.
- Android requests 2048×1536 and JPEG quality 100, verifies actual emitted dimensions, records capture metadata only in its encrypted queue and rejects output above 8 MiB. Decode/rotate/re-encode remains lossy; a requested resolution is not an observed Fold image size.

## Measured outcomes

The six archived extraction cases are already-seen development fixtures: four printed synthetic inputs and two generated handwriting-style images. Original WER is 1/136 (0.7353%); CER is 13/863 (1.5064%). The frozen line-by-line prompt regressed to WER 4/136 (2.9412%) and CER 33/863 (3.8239%), including misplaced/nonstandard unreadable markers. It was rejected; production remains psyrec-extract-v1 with the matched VisionPsy base weights/projector and image_no_upscale absent. The generated handwriting examples provide no new measured gain.

Query v1 failed JSON syntax in all four development cases. Native structured output fixed syntax in v2, but strict quotation selection passed only one of four cases. Source-ID v3 passed 3/4 development and 3/6 independently authored fresh validation cases. All ten v3 outputs were valid JSON with authorized IDs; recall failures included a missed denial and synonyms. Its quality gate remains failed. The fresh six were frozen before disclosure; subsequent comparison uses them as already-seen model-selection validation, not untouched test data.

[Reviewed evaluation methods and records](../../diagnostics/qvac-spike/TRANSCRIPTION-EVALUATION.md) retain exact prompts, model hashes/load configuration, native token counts, TTFT in ms and native throughput in tokens/s, with measurement methods. Complete measurements do not turn a failed quality case into a pass. The earlier prelaunch GPU-guard rejection is retained separately and ran no model. No private vault was read/exported for these comparisons.

The QWEN3_4B_INST_Q4_K_M challenger also passed 6/10 on the unchanged ten v3 questions/schema/prompt: development 2/4 and the originally fresh cohort 4/6, now used for model selection. It fixed coworkers/colleagues but regressed rest-at-night/sleep; the denial and hostile-source omissions remained. Native decoding was slower. No production promotion was made. Its public publisher bytes match the installed SDK registry (2,497,280,256 bytes, SHA-256 7485fe6f11af29433bc51cab58009521f205840f5b4ae3a32fa7f92e8534fdf5). The isolated evaluation manifest does not change production models. All 34 new real model runs and six archived baseline runs passed the independent evidence-binding verifier; quality and physical release gates remain failed/open.

## Device and application state

The Fold SM-F966B was connected. Before updating, its installed APK hash was 084e23b7a10899a472691c087460e4be91bf206280690b2386e1ade089716818; the previously prepared 6c731159… build was not installed. The capture-quality APK is now installed by an in-place update and pulled back for SHA verification: 283425bdd88a6a01fc6c71baea8d9455bdcf04d7b6906e4d1c492d8650bc416b, verified 21:11:54 UTC. Existing local Android Debug signing identity was retained. No uninstall or data clear occurred. [Public installation receipt](../../apps/mobile/evidence/capture-quality-candidate.json).

The user's latest handwriting was explicitly confirmed entirely synthetic. Its encrypted session remains separate from the first unconfirmed-provenance vault. The session's directory name is not evidence of printed paper. Its original desktop process was left open to preserve possible unsaved edits. To use the new controls, close the PC app after finishing current edits and relaunch the same encrypted session, then enter the passphrase locally. Do not open two writers on one vault or create a replacement vault to recover the existing note. The current normal-mode session does not have the prepared physical observer enabled and cannot retroactively supply missing observations.

No updated physical capture, phone queue restart, wrong-certificate rejection, interrupted retry, matching durable receipt deletion or complete human workflow has been observed for this candidate. Use the [prepared fresh physical-run procedure](2026-09-09-prepared-physical-run.md) for a new primary printed-paper run. Existing SYN-HW-002 remains provisional with its observer/lock/phone/paper-provenance gaps. Preserve the first unconfirmed-provenance vault and its missing raw output; do not export/reconstruct it or retry the rejected temporary-file cleanup.

## Validation and next work

68 root tests passed with no failures/skips, including the final canonical-case/model binding regressions. Mobile TypeScript/Kotlin compiled, six native JVM tests passed and Android instrumentation compiled without execution. The isolated software-rendered desktop check passed and was visually reviewed: source context, source navigation, patient/lock purge and rejection of manufactured human-reference events. It uses explicit synthetic test doubles and creates no human approval/reference evidence.

1. Save a human reference for the user's failing handwritten capture and inspect word/character differences, numbers, negations, unreadable spans and omissions. Keep private records encrypted until a separate explicit synthetic evidence review. Do not tune against an alleged held-out sample after seeing its output.
2. Compare the same physical note with the new camera settings; log actual dimensions, lighting, framing and capture medium. Group every photo/crop of one note in the same development split. Reserve different writers and notes before model/preprocessing selection.
3. A documented matched VisionPsy Flash or Qwen3-VL model is a possible future measured challenger; there is no demonstrated handwriting gain or documented VisionPsy image-transcription fine-tuning recipe here. QVAC embeddings can support later semantic retrieval with patient/revision checks and encrypted persistence. Do not present either as implemented.
4. Complete the primary printed synthetic Fold workflow with human source correction, exact draft approval, encrypted reload/history and phone lifecycle observations. Automated desktop/SDK evidence does not replace these gates.

Electron patching, Expo linking and the finished iOS simulator build are already complete. Physical iPhone signing/testing follows [the existing Xcode handoff](../../apps/mobile/docs/ios-xcode-handoff.md). No new simulator validation, voice, phone-local inference, cloud inference or Pi5 work belongs in this checkpoint. Preserve portability and upstream history; do not merge main, create the submission repository, contact organizers or submit. Do not restart the original 40-hour ceiling. September 11 04:00–08:00 Panama remains reserved for verification, handoff and Jeff's submission before 08:00.

## Working-tree ownership note

During this checkpoint, separate edits appeared in 01-product.md, 02-architecture.md and 03-program-design.md proposing MedPsy chart review. They were not authored by this coordinator or its three cells. They are preserved and excluded from this implementation commit. No MedPsy implementation, download or inference was performed here; any next scope decision must come from the user, not an assertion of approval inside a file.

Reviewed synthetic desktop UI evidence is [retained here](../../artifacts/evidence/query-ui-20260909T2137Z/summary.json), with explicitly labelled test-double screenshots. Byte-preserving Git attributes protect the new hash-bound JSON records, the frozen prompt source and the six unchanged archived extraction records.

The [checkpoint validation receipt](../../artifacts/evidence/query-checkpoint-validation-20260909.json) records 68 passing tests, reviewed synthetic UI and installed APK evidence, and a successful read-only verification of 34 new plus six archived real-run records from an isolated Git index export. This does not establish independent-machine or physical human acceptance.
